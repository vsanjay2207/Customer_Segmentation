# Customer Segmentation using K-Means Clustering

A machine learning project that groups customers based on age and spending patterns using K-Means clustering algorithm.

## Features
- Generates synthetic customer data
- Applies K-Means clustering
- Visualizes customer segments
- Makes predictions for new customers

## How to Run
```bash
pip install scikit-learn matplotlib numpy
python customer_segmentation.py